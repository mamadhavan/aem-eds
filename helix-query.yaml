version: 1

indices:
  pages:
    include:
      - '/contactUs/**'
    target: query-index.json
    properties:
      title:
        select: meta[property="og:title"]
        value: attribute(content)
